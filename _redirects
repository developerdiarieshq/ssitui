# Netlify redirects for Single Page Application
# This file handles client-side routing for Vue Router on Netlify

# Redirect all routes to index.html for client-side routing
/*    /index.html   200

# Optional: Redirect old hash routes to clean URLs (if migrating from hash mode)
# /#/*    /:splat   301
